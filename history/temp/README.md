# Temp（重要上下文临时寄存区）

用途：当你想到一段**很重要**但暂时不确定归属（需求/架构/术语/bug/想法）的内容时，先放这里，避免丢失。

## 使用建议

- 每条临时内容建议用日期开头，便于排序与回溯：`YYYY-MM-DD ...`
- 当内容已迁移到正式位置（`requirements/`、`architecture/` 等）时，在原条目下标记：**已迁移 → <目标文件>**

## 可放的内容示例

- 突然确定的"参考面定义"
- 发现某类 CP2K 输出在不同版本下格式不同
- 新增一个分析量的灵感，但还没确定实现路径
